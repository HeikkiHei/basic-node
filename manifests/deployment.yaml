apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  namespace: workshop
  name: HeikkiHei-basic-node
  labels:
    app: HeikkiHei-basic-node

spec:
  progressDeadlineSeconds: 600
  replicas: 1
  selector:
    matchLabels:
      app: HeikkiHei-basic-node

  template:
    metadata:
      labels:
        app: HeikkiHei-basic-node
      annotations:
    spec:
      containers:
        - image: gcr.io/unity-ads-workshop-test/HeikkiHei-basic-node:latest
          name: HeikkiHei-basic-node
          imagePullPolicy: IfNotPresent
          resources:
            requests:
              cpu: 0.1
              memory: 128Mi
            limits:
              cpu: 0.5
              memory: 1024Mi

      nodeSelector:
        service: workshop